'use babel';

import DaftarNamaSlotOnlineGacorView from '../lib/daftar-nama-slot-online-gacor-view';

describe('DaftarNamaSlotOnlineGacorView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
